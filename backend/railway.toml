[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements_agents.txt"

[deploy]
startCommand = "uvicorn main_agents:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
